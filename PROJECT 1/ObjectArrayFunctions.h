#ifndef OBJECT_ARRAY_FUNCTIONS_H
#define OBJECT_ARRAY_FUNCTIONS_H

// The pay rate at which the truck drivers are paid per mile
const double PAY_RATE_PER_MILE = 15;

// The truck driver count, maximum to the array
const int TRUCK_DRIVER_COUNT = 4;

// Bounds at which the driver can drive
const double MAX_DRIVING_BOUNDS_X = 10;

// Bounds at which the point can be made at
const double MAX_DRIVING_BOUNDS_Y = 10;

// Bounds at which the point can be made at
const double MIN_DRIVING_BOUNDS_X = 0;

// Bounds at which the point can be made at
const double MIN_DRIVING_BOUNDS_Y = 0;

#endif
